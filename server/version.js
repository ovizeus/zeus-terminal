// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.25',
    build: 51,
    date: '2026-04-19',
    changelog: 'Post-v2 batch6 b51 v1.7.25 — Bug #3 HOTFIX: duplicate manual live order (single click → 2 exchange orders, 1 phantom row on Zeus). Root cause: concurrent submits (client double-fire, two tabs) pass sync duplicate-position guard at serverAT.js:2166 BEFORE await binance.placeOrder(...) resolves; Binance ONE-WAY mode merges same-symbol+side into one position, but server records 2 seqs. Client sees 1 visible row (last write wins in UI), 1 hidden phantom seq. When user closes the visible row on exchange, reconcile loop finds the phantom server record and reclassifies it to Manual. 3-step HOTFIX: (1) STEP 1 (4441fce) client/src/data/marketDataTrading.ts _executeLiveManualOrder — module-level _liveOrderInFlight boolean + _lastLiveOrderCompleteTs timestamp + 750ms _LIVE_ORDER_COOLDOWN_MS. Sync gates at function entry reject if inFlight or within cooldown; finally block resets both. Fast-path UX, no network round-trip for the reject. (2) STEP 2 (7b8d165) server/routes/trading.js — per user+exchange+symbol+side _orderLocks Map with sync test-and-set acquire BEFORE try, release in finally. Lock busy → 409 + invalidate idempotency cache for clean retry. 15s janitor evicts >30s orphans. Strict per-user (req.user.id) and per-exchange (req.exchangeMode/baseUrl) — no cross-user or cross-mode bleed. Opening orders only (close/reduceOnly scoped by order id). Server is the source of truth; client guard is fast-path only. (3) STEP 3 (8f6b821) server/services/serverAT.js reconcileLivePositions — group userLivePositions by symbol+side BEFORE exchange verification; for groups 2+, keep lowest seq (first registered), canonical _closePosition(RECON_PHANTOM_MERGED_DUP, pnl=0) each extra with _closingGuard + Telegram notify + SAT_RECON_PHANTOM_MERGED_DUP audit. Self-heals any past phantom or future escape on next sync tick. Test sweep: tsc 0 errors, vitest 76/76, vite build OK, server syntax OK both files. Previous: batch5 b50 v1.7.24 (Phase 5 + Bug #2).'
};
