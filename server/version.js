// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.6.29',
    build: 22,
    date: '2026-04-18',
    changelog: 'Post-v2 batch3-G b22 — cache-proof z-index fix pentru LIQ/SUPREMUS/S-R/LLV settings modals (apar în spate la gear click). Context: batch3-E b20 a bumpat .mover la z-index:9000 în app.css dar userul a raportat în continuare modals behind panel pe mobil — probabil cache browser / service-worker servește CSS vechi cu z-index:1000. Fix cache-proof: ModalOverlay.tsx primește acum prop opțional zIndex? care se aplică ca inline style (inline bate CSS cached). LiqSettingsModal (mliq), LLVSettingsModal (mllv), SupremusModal (mzs), SRSettingsModal (msr) trimit acum zIndex={9500} — identic cu .ind-settings-modal (Ichimoku path) ca să aibă același comportament. Nu am schimbat flow, nu am schimbat logica de deschidere, doar layering. Previous: batch3-F b21 mobile update-banner scroll fix (flex column max-height:85vh, desc scrollează independent). LIQ/LLV/SUPREMUS settings modals opened BEHIND the Select Indicator panel. .mover (modal overlay) was z-index:1000, .ind-panel was z-index:8000, so every mover-based modal (mliq, mllv, mzs, msupremus, ovi) opened behind the panel while generic indicator settings (z-index 9500) opened in front. Bumped .mover to z-index:9000 — now above ind-panel (8000) and below ind-settings-modal (9500) and cmd-overlay (99999). Previous: batch3-D b19 overlay root-cause fix (reuse React chart, stop creating invisible ghost chart). Previously initCharts() in marketDataChart.ts unconditionally called LightweightCharts.createChart(el("mc")) a second time, appending a ghost canvas as a sibling of React TradingChart inside #mc. Because #mc has overflow:hidden;height:400px, the second canvas was clipped invisible. w.mainChart pointed to the invisible chart, so every overlay (renderHeatmapOverlay, renderSROverlay, renderZS, renderLiqLevels, renderOviLiquid) silently drew into a hidden canvas. Fix: initCharts now detects an existing w.mainChart (registered by TradingChart via chartBridge) and reuses it; series creation is idempotent (only creates ema50S/ema200S/wma20S/wma50S/stS/volS when not already registered), always creates the Zeus-specific ema3S/ema4S extensions, and guards the subscribeVisibleLogicalRangeChange hook behind _chartSyncInstalled so it fires only once. CVD chart is also guarded by !w.cvdChart to stay idempotent across hot-reloads. Previous: batch3-C b18 EMA p3/p4 + overlay-toggle hardening. IND_SETTINGS.ema now has p1/p2/p3/p4 in both client state.ts and public/legacy state.js; openIndSettings shows all four Period inputs (labels p3=Period 3, p4=Period 4). marketDataChart.initCharts creates two new line series (w.ema3S #00ff88, w.ema4S #ff66cc) and renderChart feeds them from IND_SETTINGS.ema.p3/p4 (0 = hidden). engine/indicators applyIndVisibility toggles ema3S/ema4S visibility with ema. phase1Adapters seeds w.ema3S/w.ema4S = null before initCharts runs to keep typeof checks safe. PanelShell chart legend shows EMA P1/P2/P3/P4 with matching colors. ChartControls togOvr now trusts legacy w.S.overlays[key] as source of truth (reads the new value AFTER togOvrFn runs so React state can never lag behind the legacy store), and the sync useEffect mirrors w.S.overlays → React store every 2s so toggles that mutate only the legacy side (persistence, cross-module bumps) show up in the React UI within ≤2s. Previous builds 8-17.'
};
