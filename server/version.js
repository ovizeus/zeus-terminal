// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.7',
    build: 33,
    date: '2026-04-18',
    changelog: 'Post-v2 batch3-R b33 v1.7.7 BIOMETRIC UNLOCK + SECURITY NUDGE — professional-app security UX on Android. (1) New custom Capacitor plugin ZeusBiometric (android/app/src/main/java/com/zeus/terminal/biometric/ZeusBiometricPlugin.java) wraps AndroidX BiometricPrompt API. Exposes isAvailable() and authenticate({title,subtitle,cancelLabel}) → WebView reaches it via window.Capacitor.Plugins.ZeusBiometric, mirroring the existing ZeusWidget pattern (no npm dep on client). build.gradle adds androidx.biometric:biometric:1.1.0; MainActivity.java registers the plugin alongside ZeusWidget. USE_BIOMETRIC manifest permission was already present. versionCode 28→33, versionName 1.7.2→1.7.7. (2) client/src/services/biometric.ts — thin JS wrapper with web/no-plugin fallbacks; per-device opt-in stored in localStorage key zeus_biometric_enabled (so enabling on phone A does not auto-unlock on phone B). (3) BiometricToggle in SettingsHubModal: new "BIOMETRIC UNLOCK" section below PIN, only rendered on native with plugin present. Enabling requires a live fingerprint verification — if the user cannot pass the prompt right now, the toggle cannot be flipped. Disabled until PIN is set (biometric is a shortcut into the PIN gate, not a replacement). (4) PinLockScreen auto-triggers the fingerprint prompt on mount when biometric is enabled + available; also shows a manual "USE FINGERPRINT" button so the user can retry after cancelling. Successful prompt → pinMarkUnlockedFromBiometric() writes the same sessionStorage unlock flag as typed-PIN verification. (5) SecurityNudgeModal: first-run modal on Android launches when neither PIN nor biometric is configured. Suggests activating PIN (and fingerprint if the device supports it) with "Activate now" (opens Settings) vs "Remind me later" (48h snooze in localStorage). Triggers 4s after boot on native only; web users are never shown this modal. Zero changes to AT engine, brain, DSL logic, trade execution, server auth routes. APK rebuild required for native biometric path; web deploy delivers the UI shells and no-ops gracefully on older APKs. Previous: batch3-Q b32 PIN gate rebuild (React component + sessionStorage).'
};
