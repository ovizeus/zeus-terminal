// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.62',
    build: 88,
    date: '2026-04-21',
    changelog: [
        'Post-v2 batch40 b88 v1.7.62 — Phase 2 S2.C GLOBAL PANIC COMPLETION. S2 introduced PANIC at brain-driven entry only (processBrainDecision + _executeLiveEntry). S2.C closes the gap on every manual/server path that can create NEW live exposure. Strict additive patch, no schema migration, no UI, no DSL/Brain/Bybit touch. Gates added (each fails fast with 423 and audit-log entry; reason: GLOBAL_HALT active — new live exposure blocked): (C1) POST /api/order/place after tradingEnabled check, before order lock/risk/duplicate guard — logger.warn "Order blocked — GLOBAL_HALT", audit ORDER_BLOCKED_GLOBAL_HALT. (C2) serverAT.registerManualPosition when mode === "live" — blocks before seq allocation, returns {ok:false, error:"GLOBAL_HALT active — new live exposure blocked"}; DEMO path intentionally unaffected (no real risk). (C3) POST /api/order/modify after tradingEnabled — cancel+replace LIMIT POSTs a new order that can change exposure; audit ORDER_MODIFY_BLOCKED_GLOBAL_HALT. (C4) POST /api/addon before calling serverAT.addOnPosition — add-on is unambiguously new exposure (more margin/risk); audit ADDON_BLOCKED_GLOBAL_HALT. Left permissive by design: POST /api/manual/protection (attaches SL/TP to existing live position; no new size; PANIC must not block protective actions that reduce/manage existing risk). No other server-side endpoint surveyed (server.js + all server/routes/*.js files) can create new live exposure outside these 4 paths. Tests: jest 161/161 green, probe-s2.js 29/29 green (S2 non-regression), probe-s2c.js 10/10 green covering T1 (DEMO manual allowed under PANIC), T2 (LIVE manual blocked + GLOBAL_HALT in error), T3 (LIVE allowed after disarm), T4 (brain gate still blocks), T5 (post-disarm flow + S2.A decisionId intact). HTTP smoke against live PM2 process: /api/order/place, /api/order/modify, /api/addon all 423 while halted + /api/manual/protection still reachable + S1 /api/at/resume intact. Files: server/routes/trading.js (+23 LOC across 3 gates), server/services/serverAT.js (+6 LOC in registerManualPosition), server/version.js. Backups: *.bak.s2c.',
        'Previous: b87 v1.7.61 — Phase 2 S2 IDEMPOTENCY + GLOBAL PANIC HALT. Two strict, additive server-side changes; zero changes to Brain / DSL / decision semantics / schema / client. (S2.A) Decision idempotency end-to-end on the brain-driven live-entry path. processBrainDecision stamps each entry with a crypto-random 8-hex decisionId (32 bits entropy × per-user seq = no realistic collision). _executeLiveEntry now derives newClientOrderId = `SAT_<seq>_<decisionId>` (stable, ≤36 chars for Binance limit) instead of `SAT_<liveSeq>_${Date.now()}` — retries (in-process or post-restart) produce the SAME token so Binance dedups at the exchange, returning the original order instead of creating a duplicate. Immediately after MAIN order POST succeeds, entry.live = { status:"MAIN_PLACED", mainOrderId, clientOrderId, decisionId } is persisted BEFORE SL/TP attempts, so a crash between MAIN and SL cannot lose the idempotency key. On rehydrate-and-reexecute, a short-circuit check (entry.live.mainOrderId already set) aborts re-placement. decisionId is also carried into entry.live at final LIVE/LIVE_NO_SL assignment. Additive JSON only — no schema migration (persisted inside at_positions.data blob). (S2.B) Global PANIC halt — cross-user entry kill switch persisted in at_state under key "global:halt" (TEXT PRIMARY KEY ⇒ single canonical row; user_id NOT NULL records the admin who toggled). serverAT exports isGlobalHaltActive()/getGlobalHaltState()/setGlobalHalt(active,byUserId,reason). Read-failure defaults to HALTED (fail-safe — matches S1.C fail-fast pattern). Gate inserted at TWO points: (1) top of processBrainDecision (before _uState, cheap short-circuit when halted, emits missed_trade reason=GLOBAL_HALT); (2) top of _executeLiveEntry after atActive re-check (blocks any in-flight live attempt even if decision already passed; zombie-cleans the entry with closeReason=GLOBAL_HALT_INFLIGHT). New admin-only REST endpoints: GET /api/panic (any authed user reads state); POST /api/panic body {active:boolean,reason?:string} — role==="admin" gate, userId from JWT never body. Survives restarts because the DB row is durable and every call re-reads (no stale in-memory cache). Scope kept strict: no SERVER_BRAIN/SERVER_AT flag flipped, no Bybit path changes, no DSL changes, no UI labels, no schema migration, no parity harness, no positions.changed WS. Tests: 161/161 jest green, client tsc+vite build green, probe-s2.js 29/29 green covering T1 (decisionId format, stable clientOrderId, dup guard), T2 (collision safety per user/symbol/side, opposite-side allowed), T3 (halt arm/disarm, processBrainDecision blocked while halted, module-reload persistence, byUserId guard), T4 (no S1 regression — getFullState + post-disarm flow). Files: server/services/serverAT.js, server.js, server/version.js, tests/probe-s2.js. Backups: *.bak.s2.',
        'Previous: b86 v1.7.60 — Phase 2 S1 FOUNDATION (Warm-start + Resume + Mutex FAIL). (S1.A) WS at_update warm-start on accept. (S1.B) GET /api/at/resume canonical per-user rebuild endpoint. (S1.C) migrationFlags.js startup FAIL on mutex violation + set() rejects instead of coerce. Files: server.js, server/migrationFlags.js, server/version.js.',
        'Previous: b85 v1.7.59 — Phase 12.A Batch H CLEANUP (2 last hardcoded Binance fallbacks removed; exchange/env UX stack A–H closed).',
        'Previous: b84 v1.7.58 — Phase 12.A EXCHANGE / API UX STACK (Batches A–G). End-to-end elimination of hardcoded "Binance" identity across the app: every surface now derives exchange + env from server truth. Batch A: typed WS frame "exchange.changed" emitted by /api/exchange/{save,disconnect,verify} and as warm-start on every WS accept (server.js, server/routes/exchange.js, client/src/types/sync.ts). Batch B: useServerSync maps exchange.changed + at_update payload into useUiStore (activeExchange, executionEnv, exchangeMode, apiConfigured, executionBlockedReason). Batch C: hardcoded "binance" defaults removed from positionsStore.liveExchange + TP.liveExchange + logout reset — type widened to "binance"|"bybit"|null. Batch D: dynamic exchange labels on AutoTradePanel (D1), ModeBar (D2), ManualTradePanel header + balance (D3), StatusBar (D4 — LIVE→REAL display map + exchange suffix), PositionRows + ARES PositionsList chips (D5). Batch E: global Header badge (E1) reading engineMode + executionEnv + activeExchange; SettingsHubModal conflict previews tightened (E2 — "SAVE DISABLED —" prefix + ENV_CONFLICT hint on connected cards); REAL disconnect confirm differentiated with 4-bullet warning (E3). Batch F: REAL manual order pre-place confirm dialog in marketDataTrading.ts — detailed message with side/symbol/size/leverage/entry-type/price/TP/SL + derived exchange label, TESTNET/DEMO/LOCKED skip. Batch G: history snapshot of exchange + env at open — serverAT stamps both entry sites using _resolveExecutionEnv + getExchangeCreds; journal.js GET projects them, POST /report whitelists them; JournalEntry type + useServerSync.pullJournal + storage reporter + JournalPanel display + CSV export all carry the snapshot through to UI. Zero schema migration, zero touches on Brain / DSL / AT engine / exchange adapters / engine/indicators.ts. All UI strings in English. Files: server.js, server/{services/serverAT,routes/{exchange,journal}}.js, client/src/{types/{sync,orderflow},stores/{uiStore,positionsStore},core/state,hooks/useServerSync,services/storage,data/marketDataTrading,components/{layout/{Header,ModeBar,StatusBar},dock/{AutoTradePanel,ManualTradePanel,PositionRows,ares/PositionsList},modals/SettingsHubModal,advanced/JournalPanel}}.{ts,tsx}. Backups: *.bak.b84–b90.',
        'Previous: b83 v1.7.57 — Phase 11.9 MARKET RADAR MOBILE MARQUEE FINAL FIX (JS rAF).',
        'Previous: b82 v1.7.56 — Phase 11.8 MARKET RADAR MOBILE MARQUEE FIX (mask-image → pseudo-elements).',
        'Previous: b81 v1.7.55 — Phase 11.7 MARKET RADAR BUGFIX (radarCache + snapshot warm-start + mobile marquee).',
        'Previous: b80 v1.7.54 — Phase 11 UI SURGICAL MOVE + MARKET RADAR. b79 v1.7.53 — DSL PER-MODE + TOAST HOTFIX. b78 v1.7.52 — BRAIN/AT/DSL FULL PER-MODE SPLIT. b77 v1.7.51 — CHART DRIFT HOTFIX REVERT B73. b76 v1.7.50 — BRAIN SPLIT SECOND SWITCH PATH FIX. b75 v1.7.49 — BRAIN MODE SYNC HOTFIX. b74 v1.7.48 — BRAIN DEMO/LIVE NAMESPACE SPLIT. b73 v1.7.47 — CHART UX PACK.'
    ].join(' '),
};
