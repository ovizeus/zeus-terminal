// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.48',
    build: 74,
    date: '2026-04-20',
    changelog: 'Post-v2 batch28 b74 v1.7.48 — Phase 10.18 BRAIN DEMO/LIVE NAMESPACE SPLIT. Complete separation of Brain configuration per AT trading mode so profile and bmMode persist independently for demo vs live. (1) TRADING BADGE MODE BUG FIX: engine/brain.ts:1756 previously hardcoded `getDemoPositions()` when computing hasPos for the ARM badge — when the user was on LIVE (zero open positions) but DEMO held open AT positions, the badge stayed lit "TRADING" because it always read the demo array. FIX: `const _atMode = getATMode(); const _positionsForMode = _atMode === "live" ? getLivePositions() : getDemoPositions();` so the badge follows the active mode. `getLivePositions` + `getATMode` were already imported from stateAccessors. (2) USER_SETTINGS.brain NAMESPACE: core/config.ts adds `brain: { live: {profile, bmMode}, demo: {profile, bmMode} }`. Top-level flat `profile`/`bmMode` kept as active-mode snapshot for wire-compat and first-run migration seed. New helpers: `_currentATModeKey()` resolves active mode via getATMode() fallback demo; `_ensureBrainNamespace()` defensively initialises subtrees; `_seedBrainFromFlat()` copies pre-split flat values into both namespaces on first boot so users don''t lose existing settings; exported `applyBrainCfgForMode(mode)` pushes namespace→S.profile/S.mode/BM.mode/brainStore + refreshes profile/mode button classes. `_usSave` now writes `USER_SETTINGS.brain[currentMode] = {profile, bmMode}` alongside the flat keys. `_usApply` calls `_seedBrainFromFlat()` then `applyBrainCfgForMode(_currentATModeKey())`. `_usApplyFlatToUserSettings` accepts incoming `flat.brain` and merges per-mode. `loadUserSettings` (localStorage path) hydrates `parsed.brain`. `w.applyBrainCfgForMode` exposed globally. (3) MODE SWITCH WIRING: data/marketDataTrading.ts _executeGlobalModeSwitch — before flipping AT._serverMode, calls `w._usFlush()` so the outgoing mode''s current profile/bmMode lands in its own namespace (prevents 800ms-debounce leak). After the flip, calls `w.applyBrainCfgForMode(newMode)` so S/BM/brainStore reflect the new mode''s saved values. The existing `runDSLBrain()` + atPollOnce + `atStateChanged` CustomEvent continue to run, so the UI re-renders with the correct per-mode state. (4) SERVER WIRE + SETTINGS STORE: settings-contracts.ts SettingsPayload extended with optional `brain?: { live?: {profile?, bmMode?}; demo?: {profile?, bmMode?} } | null`. stores/settingsStore.ts — LegacyUserSettings extended with `brain?` field; `_projectFromLegacy()` adds `brain: us.brain`; `_projectToLegacy()` merges `settings.brain.live/demo` back into `us.brain`. server/routes/trading.js SETTINGS_WHITELIST adds `brain` so POST /api/user/settings passes the nested object through to SQLite user_settings.data blob (no schema change required — the blob is free-form JSON). (5) GOTCHAS HANDLED: on first boot after the split ships, `_seedBrainFromFlat()` copies pre-existing flat `profile`/`bmMode` into BOTH namespaces, so users don''t see "reset" settings — they see the same values in both modes, then diverge them by toggling on each mode. Debounced saves flushed on mode switch so no state is lost. Regime/structure/phaseFilter/liqCycle etc. stay global (runtime-computed from market data, not mode-specific). AT risk rules (TC/ATStore) stay global — same risk posture in both modes. Positions (TP.demoPositions / TP.livePositions) were already split. Backups: brain.ts.bak.b74, config.ts.bak.b74, brainStore.ts.bak.b74, settingsStore.ts.bak.b74, marketDataTrading.ts.bak.b74, trading.js.bak.b74, version.js.bak.b74. Previous: batch27 b73 v1.7.47 — Phase 10.17 CHART UX PACK (candle type selector + premium session overlays + chart settings persist + price axis width + LLV rename + RO→EN sweep). Previous: batch26 b72 v1.7.46 — Phase 10.16 DESKTOP CHART WIDTH DRIFT FIX. Previous: batch25 b71 v1.7.45 — Phase 10.15 REGIME HISTORY 500 FIX. Previous: batch24 b70 v1.7.44 — Phase 10.14 LIQUIDITY MAGNET PANEL EN-ONLY. Previous: batch23 b69 v1.7.43 — Phase 10.13 SECURITY HARDENING. Previous: batch22 b68 v1.7.42 — Phase 10.12 ADAPTIVE TOGGLE TRULY WIRED. Previous: batch21 b67 v1.7.41 — Phase 10.11 ADAPTIVE CONTROL UI SYNC TO BRAIN STORE. Previous: batch20 b66 v1.7.40 — Phase 10.10 BRAIN PANELS COLLAPSE STATE PERSIST. Previous: batch19 b65 v1.7.39 — Phase 10.9 BRAIN DASHBOARD REFLECTION SEED ON BOOT. Previous: batch18 b64 v1.7.38 — Phase 10.8 AT SAVE-SETTINGS TOAST FIX + LEVERAGE CUSTOM INPUT + ADDON LOG CLEANUP. Previous: batch17 b63 v1.7.37 — Phase 10.7.1 ADD-ON currentPrice FORWARDING. Previous: batch16 b62 v1.7.36 — Phase 10.7 AT ADD-ON BUTTON FIX + USER-CHOSEN AMOUNT MODAL. Previous: batch15 b61 v1.7.35 — Phase 10.6 HOTFIX fmt IS NOT DEFINED IN AT RENDER. Previous: batch14 b60 v1.7.34 — Phase 10.5 PTR STACKED TWO-LINE LABEL. Previous: batch13 b59 v1.7.33 — Phase 10.4 PTR INDICATOR VISUAL CLEANUP. Previous: batch12 b58 v1.7.32 — Phase 10.3 PULL-TO-REFRESH IN PANELS RESTORED.',
};
