// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.56',
    build: 82,
    date: '2026-04-20',
    changelog: [
        'Post-v2 batch34 b82 v1.7.56 — Phase 11.8 MARKET RADAR MOBILE MARQUEE FIX. Bug reported on b81: pe telefon benzile Market Radar nu se mișcau deloc, desktop OK. Root cause: .mr-band avea mask-image (soft edge fade) — Safari iOS are un bug cunoscut în care un părinte cu mask-image face copilul cu animație translate3d să cadă de pe compositor path și animația apare "blocată". Fix: eliminat -webkit-mask-image/mask-image de pe .mr-band, înlocuit cu pseudo-elemente ::before/::after (overlay gradient 24px la stânga și dreapta) care păstrează vizualul edge-fade fără a afecta layer-ul animat. Adăugat transform:translateZ(0) pe .mr-band pentru compositor promotion explicită, plus CSS shorthand "animation: mr-scroll 40s linear infinite" ca fallback în caz că PWA shell-ul strică inline animationDuration. Zero touches pe date/logica radar. Files: client/src/components/radar/MarketRadar.css. Backup: MarketRadar.css.bak.b82.',
        'Post-v2 batch33 b81 v1.7.55 — Phase 11.7 MARKET RADAR BUGFIX. Fixes reported on v1.7.54: (1) refresh emptied the radar and it took minutes to repopulate; (2) phone + browser saw different events; (3) mobile marquee stalled; (4) first-paint warm-start too slow. Added /server/services/radarCache.js — shared in-memory ring cache (50 per color, 10-min TTL) that both marketRadar.js and liquidationFeed.js push to on every emit. server.js WS connection handler now sends a {type:"market.radar.snapshot"} frame to every new/reconnecting socket carrying {green, red, lastEventTs} — so refresh, reconnect, second-tab open and second-device connect all warm-start from the same canonical cache instead of waiting for the next live tick. Client: added WsMarketRadarSnapshot type, marketRadarStore.hydrate() that atomically replaces both queues + lastEventTs with validation + cap, and marketRadarRealtime now routes "market.radar.snapshot" → hydrate before any live event. Mobile marquee fix: wrapped .mr-band:hover pause rule in @media (hover:hover) and (pointer:fine) — touch devices no longer stick in paused state after a tap. Added backface-visibility:hidden + translate3d keyframes so iOS Safari promotes the track to its own compositor layer (mask-image no longer throttles the animation). Zero touches to Brain/AT/DSL/Manual/settings-split. Files: server.js (+snapshot send), server/services/{radarCache,marketRadar,liquidationFeed,version}.js, client/src/{types/sync,stores/marketRadarStore,services/marketRadarRealtime,components/radar/MarketRadar.css}.ts/.css. Backups: *.bak.b81.',
        'Post-v2 batch32 b80 v1.7.54 — Phase 11 UI SURGICAL MOVE + MARKET RADAR. Phase 11.1: relocated <ModeBar /> from above the chart to between chart legend and ZeusDock (+ bootstrapInit no longer moves React-owned IDs). Phase 11.2: server Market Radar scanner — 60s poll of Binance Futures top 300 USDT perpetuals by 24h quoteVolume, emits spike/dump 1h/4h/24h, volSpike, rankUp/rankDown, newTop300/exitTop300 via /ws/sync as type:"market.radar". Phase 11.2.1: feature flags MARKET_RADAR_ENABLED + MARKET_RADAR_POLL_MS + honest "TOP 300 BINANCE USDT · 24h VOL" labeling. Phase 11.3: client useMarketRadarStore (dual capped FIFO, green/red) + marketRadarRealtime WS subscriber. Phase 11.4: server expansion — BTC-relative delta + 15min streak counter attached to every event, MARKET_RADAR_FUNDING_ENABLED (|rate|≥0.05%/8h → fundingExtreme), MARKET_RADAR_OI_ENABLED (|Δ|≥10% vs 1h → oiSurge), and MARKET_RADAR_LIQ_ENABLED persistent WS to !forceOrder@arr ($100k+ liquidations → liqLong/liqShort). Phase 11.4.2 + 11.5: MarketRadar.tsx dual-marquee panel with click-to-chart (switchWLSymbol parity), hover-to-pause, reduced-motion support, 10s halo for newTop300. Isolation: zero AT / DSL / Brain / Manual / trading-path touches. All UI strings in English. Files: client/src/components/{layout/ModeBar,layout/PanelShell,radar/MarketRadar}.{tsx,css}, client/src/{stores/marketRadarStore,services/marketRadarRealtime,core/bootstrapInit,types/sync}.ts, server.js (wsBroadcastAll helper + RADAR/LIQ boot hooks), server/services/{marketRadar,liquidationFeed,logger}.js.',
        'Previous: b79 v1.7.53 — Phase 10.20.1 DSL PER-MODE + TOAST HOTFIX. b78 v1.7.52 — Phase 10.20 BRAIN/AT/DSL FULL PER-MODE SPLIT. b77 v1.7.51 — Phase 10.19 CHART DRIFT HOTFIX REVERT B73. b76 v1.7.50 — Phase 10.18.2 BRAIN SPLIT SECOND SWITCH PATH FIX. b75 v1.7.49 — Phase 10.18.1 BRAIN MODE SYNC HOTFIX. b74 v1.7.48 — Phase 10.18 BRAIN DEMO/LIVE NAMESPACE SPLIT. b73 v1.7.47 — Phase 10.17 CHART UX PACK.'
    ].join(' '),
};
