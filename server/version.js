// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.43',
    build: 69,
    date: '2026-04-19',
    changelog: 'Post-v2 batch23 b69 v1.7.43 — Phase 10.13 SECURITY HARDENING (4 items, user-authorized) — (1) NPM AUDIT FIX: resolved 5 vulnerabilities (3 high: path-to-regexp ReDoS, basic-ftp CRLF injection + DoS, @xmldom/xmldom XML injection; 2 moderate: nodemailer SMTP CRLF, brace-expansion hang). `npm audit fix` lifted transitive pins via package-lock.json only; no package.json changes, no major bumps. Post-fix: 0 vulnerabilities, 595 packages audited, tests 161/161 server + 79/79 client pass. (2) FAIL2BAN INSTALLED: apt-get install fail2ban v1.0.2 + custom filter /etc/fail2ban/filter.d/zeus-auth.conf (datepattern parses JSON "ts" ISO timestamp, failregex matches component=AUTH with embedded "LOGIN_FAILED ... ip=<HOST>"; fail2ban-regex test passes 1/1) + jail /etc/fail2ban/jail.d/zeus.conf (5 failed logins in 900s → 1h ban via iptables). systemctl enabled + active; zeus-auth jail tails /root/zeus-terminal/data/logs/zeus.log. NO ssh hardening (per feedback_no_ssh_hardening.md). (3) LOGIN ATTEMPTS PERSISTED TO SQLITE: in-memory Maps loginAttempts (per-IP) + loginAttemptsEmail (per-email) in auth.js wiped on every pm2 reload — an attacker who tripped the 10/IP + 5/email 15-min window could reset it by forcing a restart. FIX: new migration 026_login_attempts creates table login_attempts(key TEXT, kind TEXT, count INT, reset_at INT, PRIMARY KEY(kind, key)) + idx_login_attempts_reset. database.js exposes loginAttemptGet / loginAttemptUpsert / loginAttemptDelete / loginAttemptPruneExpired. auth.js replaces both Maps with a single _bumpLoginAttempt(kind, key, max) helper that reads the row, bumps count atomically via UPSERT, and honors the same 15-min window + 10/5 ceilings. Same cleanup interval (5min) now calls db.loginAttemptPruneExpired(now) to GC stale rows. Behavior is unchanged to users — only the storage moved from volatile RAM to WAL-backed SQLite. (4) FAIL2BAN-READABLE AUTH LOGS: LOGIN_FAILED events formerly only landed in audit_log SQLite (invisible to fail2ban). Added two logger.warn calls in auth.js that embed ip= into the msg field for three failure modes: unknown_email (probe), wrong_password (brute force), temp_password_expired (admin-reset bypass attempt). Email is still masked via _mask(); IP is plain so the filter can ban the source. (5) .ENV PERMISSIONS: already 600 root:root — verified, no change. Net effect: brute-force resistance hardened at THREE layers — rate-limit (unchanged, but now durable), fail2ban (new, banlist IP at firewall level), and SQLite state (new, survives restart). Backups: auth.js.bak.b69, database.js.bak.b69, version.js.bak.b69. Fail2ban configs NOT backed up (net-new). Previous: batch22 b68 v1.7.42 — Phase 10.12 ADAPTIVE TOGGLE TRULY WIRED — b67 fixed the React AdaptivePanel button, but user still saw the button dead. Deeper audit: the Adaptive dock item does NOT render the React AdaptivePanel. pageview.ts:153 intercepts dockId==="adaptive" and moves #adaptive-strip (legacy DOM) into the page view content. The strip content is populated by initAdaptiveStrip at trading/risk.ts:441, which moves children of #adaptive-sec (AnalysisSections.tsx:801) into #adaptive-strip-panel. The button the user actually taps is AnalysisSections.tsx:808 — a legacy button with `onClick={() => (window as any).toggleAdaptive?.()}` — and window.toggleAdaptive was NEVER assigned anywhere in the codebase. The optional-chain `?.()` silently no-ops when the binding is missing, so every tap was a completely silent dead click. b67s sync-to-store only helped the React button in PanelShell — which the user never sees because pageview.ts shortcircuits to the legacy strip. FIX: (1) Added `;(window as any).toggleAdaptive = toggleAdaptive` at module end of trading/risk.ts — one line, closes the missing binding. Legacy onClick now actually runs the engine-side toggle. (2) Added `tog.style.background = w.BM.adaptive.enabled ? "#0a2a1a" : "#0a1220"` inside toggleAdaptive imperative DOM update — the legacy button has hardcoded OFF colors including background; without setting background too, only borderColor+color would flip and the button would still look grey/dark even when ON. With all three style props flipping, the button visibly lights up (dark green bg, green border+text). Engine logic unchanged, brainStore sync from b67 still runs so the in-tree React AdaptivePanel would also update if ever re-enabled. Backups: risk.ts.bak.b68, version.js.bak.b68. Previous: batch21 b67 v1.7.41 — Phase 10.11 ADAPTIVE CONTROL UI SYNC TO BRAIN STORE — user reported "iconul adaptive nu functioneaza, intru, afiseaza tot, dar cand apas ON nu porneste nimic". Root cause: Phase 6 C7 made brainStore the canonical surface for MODE/PROFILE/ENTRY/FLOW/MTF/SWEEP/GATES (CANONICAL_BM_KEYS at config.ts:2060), but `adaptive` was intentionally left BACKING-ONLY (comment at config.ts:2050 lists it among pass-through keys). The legacy mutators in trading/risk.ts (toggleAdaptive, _adaptLoad, recalcAdaptive) write only to `w.BM.adaptive.*`, while the React AdaptivePanel.tsx:6 reads from `useBrainStore(s => !!s.brain.adaptive?.enabled)` — a SEPARATE object on the store that never changes. User sees the button label stuck on "ADAPTIVE OFF" no matter how many times he taps; multipliers row stays at ×1.00 even when the engine has computed new values per bucket; LS-restored state after refresh is invisible to the UI. Engine logic (reads w.BM.adaptive in sizing/entry gates) works correctly — only the UI mirror lied. FIX: three sync points, one helper. Added `_syncAdaptiveToStore()` in risk.ts that shallow-copies w.BM.adaptive into brainStore via `useBrainStore.getState().patch({ adaptive: {...} })`. Called from: (1) toggleAdaptive after the flip + _adaptSave — so the ON/OFF button label turns green/grey instantly; (2) _adaptLoad at the end of LS restore — so after F5 / app relaunch the UI reflects the persisted enabled state and saved multipliers; (3) recalcAdaptive after BM.adaptive.buckets + entryMult/sizeMult/exitMult writes — so the "Active multipliers" row and bucket table update live when the engine recomputes after trade closes. Zero change to engine read path: entry/sizing still reads w.BM.adaptive directly (backing stays authoritative). Store is a pure read-replica for UI consumption; patch is a shallow copy of 6 primitive fields + buckets ref, no cost in hot path. Backups: risk.ts.bak.b67, version.js.bak.b67. Previous: batch20 b66 v1.7.40 — Phase 10.10 BRAIN PANELS COLLAPSE STATE PERSIST — user reported Brain Vision + Brain Dashboard re-open on every refresh/app restart; he keeps them closed most of the time, so re-hiding them on every mount is friction. Both panels lived in AutoTradePanel.tsx with `useState(true)` defaults — pure in-memory React state, nothing written anywhere, so closing them only held until the component unmounted. FIX: changed both useState initializers to lazy readers of localStorage (`zeus:ui:brainVisionOpen` / `zeus:ui:brainDashOpen`, stored as "0"/"1"; missing key defaults to open so existing users see no change on first boot). Added two useEffect writes that sync the latest value back to LS on every toggle. Both reads and writes are wrapped in try/catch in case LS is blocked (private-mode, quota). Keys live under the canonical `zeus:ui:` namespace — not touching settingsStore because this is device-local UI preference, not a tradable setting worth syncing across tabs/devices. Backups: AutoTradePanel.tsx.bak.b66, version.js.bak.b66. Previous: batch19 b65 v1.7.39 — Phase 10.9 BRAIN DASHBOARD REFLECTION SEED ON BOOT — user reported Brain Dashboard "pare blocat" (appears blocked/inactive). Audit traced two architectural reasons the panel renders empty: (1) serverReflection.start() is called ONLY from serverBrain.start() at serverBrain.js:120, and serverBrain is gated by MF.SERVER_BRAIN=false — so the reflection engine never starts → _restoreAllUsers() never runs → learned rules persisted from 1949 historical AT closes are never loaded back into memory after pm2 reload. (2) thoughts[] is a pure in-memory ring buffer (MAX_THOUGHTS=100) — populated ONLY when reflectOnTrade fires at AT close time, and WIPED on every server restart. Today: last AT close 17:43, last pm2 reload 19:54 → thoughts Map is empty → client bootstrapBrainDash.ts:183 shows only "Waiting for trades to reflect on..." placeholder. Dashboard polling itself works fine (5s initial + 30s interval at bootstrapBrainDash.ts:204, /api/brain/dashboard returns 200 with empty arrays). FIX (Option B — full backfill): [1] Extracted reason-building from reflectOnTrade into internal _buildReasons(trade, isWin) helper at serverReflection.js — same logic, now reusable. [2] Added new exported seedFromHistory(userIdArg, limit) at serverReflection.js that reads last 50 closed trades per user from at_closed via _getRecentTrades, sorts chronologically (oldest first so streak semantics make sense), replays each into _tht(uid) ring buffer with the same thought shape reflectOnTrade generates, and recomputes self-score counters (decisionsToday/correctToday/streak) ONLY for trades within the last 24h cutoff so "today" metrics stay honest. Rules and anti-patterns are NOT re-derived — rules come from DB via _restoreAllUsers, avoiding duplicate _createRule calls. [3] server.js:1057 now unconditionally requires serverReflection, calls start() (idempotent via _reflectionTimer guard) + seedFromHistory(null, 50) at boot — independent of MF.SERVER_BRAIN. Logs "[REFLECTION] engine started + history seeded (dashboard ready)". When SERVER_BRAIN is later flipped on, serverBrain.start() re-calls serverReflection.start() — the guard short-circuits, no double-start. Immediate effect: after pm2 reload, dashboard shows LIVE THINKING populated with reflections from last ~50 AT closes, plus SELF-SCORE with last-24h accuracy/streak, plus LEARNED RULES from persisted DB. Backups: serverReflection.js.bak.b65, server.js.bak.b65, version.js.bak.b65. Previous: batch18 b64 v1.7.38 — Phase 10.8 AT SAVE-SETTINGS TOAST FIX + LEVERAGE CUSTOM INPUT + ADDON LOG CLEANUP — three items in one batch. (1) SAVE SETTINGS BUTTON "BROKEN": user reported it did nothing when pressed. Audit showed the save logic was fine (settingsStore.patch + saveToServer + LS writethrough) but feedback was missing — AutoTradePanel.tsx:165,168 called `w.toast(...)` (window.toast), and nobody in the codebase ever assigned window.toast. The `typeof w.toast === "function"` guard short-circuited → zero visible feedback → user assumed button was broken. FIX: imported toast from ../../data/marketDataHelpers and replaced the two w.toast() call sites with direct toast() calls. Save path was not touched — it already worked. (2) LEVERAGE CUSTOM INPUT: previously a <select> with four hardcoded options (2x/5x/10x/20x) — user had no way to trade with e.g. 15x, 25x, 50x, 125x. Replaced with four preset pill buttons (quick-click, highlighted when active) plus a free-form number input. No upper cap per user request ("sa nu fiu limitat") — Binance/server reject invalid values at order placement time. id="atLev" preserved on the input so legacy selectors in events.ts / parseInt(el("atLev")?.value) still work. (3) ADDON LOG CLEANUP: removed the three diagnostic console.log entries from /api/addon route (entry log, REJECTED log, OK log) added in b63 for triaging the currentPrice bug. Root cause is solved; logs no longer earn their keep. Kept only the console.error on caught exceptions. Backups: AutoTradePanel.tsx.bak.b64, version.js.bak.b64. Previous: batch17 b63 v1.7.37 — Phase 10.7.1 ADD-ON currentPrice FORWARDING — after b62 shipped the AddOnModal, user tested and got continuous 400 Bad Request on POST /api/addon. Server log (added as diag): "REJECTED: No current price available for profit check". Root cause at serverAT.js:2394: `const curPrice = pos._lastPrice || options.currentPrice`. pos._lastPrice is set only in the server tick loop (serverAT.js:1758), so for a freshly-opened AT position or one whose tick has not yet run, _lastPrice is undefined. Client did not forward currentPrice in the request body, so both sides of the OR were undefined → rejection. FIX: client now sends currentPrice (from modal symPrice, with fallback to allPrices[sym] / wlPrices / pos.entry) in the /api/addon POST body; route /api/addon reads req.body.currentPrice and puts it on opts.currentPrice; serverAT.js:2394 already honored options.currentPrice — no server-logic change needed, just forwarding plumbing. Also added route-level console logs at /api/addon for entry and rejection so future diagnosis does not need speculation. Backups: autotrade.ts.bak.b63, trading.js.bak.b63. Previous: batch16 b62 v1.7.36 — Phase 10.7 AT ADD-ON BUTTON FIX + USER-CHOSEN AMOUNT MODAL — user reported the AT panel ADD-ON button never worked since it shipped: tap did nothing, no toast, no modal, no server hit. Audit (no-change) traced two independent bugs. (1) WIRING ORPHAN: button rendered with data-addon-id="${pos.id}" and was wired via attachConfirmClose at autotrade.ts:1799, but events.ts:163 lookup chain only knew data-id/data-live-id/data-close-id/data-partial-id/btn.id — data-addon-id was NOT in the chain, so attachConfirmClose hit the `if (!posId) return` guard at line 165 and exited WITHOUT registering touchend/click. The button never got a listener — pressing it was a no-op. (2) UX MISMATCH: even if listener had attached, the legacy openAddOn jumped straight to POST /api/addon and the server hard-coded addOnSize = origSize * 0.5 (serverAT.js:2407) — user had no say in the amount. User wanted a modal with an input and an OK confirm. FIX: NEW client modal AddOnModal.tsx (mirrors PartialCloseModal pattern) showing original size, current size, addOn count/max, current price, PnL, plus quick-presets 25%/50%/100% of original and a numeric input. openAddOn now opens the modal; new helper execAddOn(posId, addOnSize) runs the POST after CONFIRM. Wiring at autotrade.ts:1796-1810 replaced attachConfirmClose with a direct addEventListener("click") + data-addon-attached marker (modal IS the confirmation, two-tap on the button would be redundant). Server: /api/addon route (trading.js:780) and addOnPosition (serverAT.js:2405-2415) now read options.addOnSize when provided and fall back to 50%-of-original when missing — backward-compatible with any non-modal caller. Server still validates balance/margin/maxAddon/in-profit/race-guard. Backups: autotrade.ts.bak.b62, serverAT.js.bak.b62, trading.js.bak.b62, version.js.bak.b62. Previous: batch15 b61 v1.7.35 — Phase 10.6 HOTFIX fmt IS NOT DEFINED IN AT RENDER — user reported continuous "Uncaught ReferenceError: fmt is not defined" console spam after Phase 10/10.5 ship, plus AT positions disappearing from #atActivePosPanel even though the same positions rendered fine in DSL and on the exchange. Root cause: Phase 9E1 unified the AT card markup with DemoPositionRow/LivePositionRow and introduced a Margin/Notional/Fees line at autotrade.ts:1769 that calls fmt(margin)/fmt(notional)/fmt(estFees), but the import on line 16 only pulled fP from utils/format — fmt was never imported. Every renderATPositions call threw inside the autoPosns.map() callback, so panel.innerHTML = map(...).join("") never executed and the panel stayed on its placeholder. ONE-LINE FIX: changed `import { fP } from "../utils/format"` to `import { fmt, fP } from "../utils/format"`. This single regression explains BOTH bugs F (cards missing from AT panel) and G (console error flood) — same exception, two visible symptoms. Backups: autotrade.ts.bak.b61, state.ts.bak.b61, version.js.bak.b61. Previous: batch14 b60 v1.7.34 — Phase 10.5 PTR STACKED TWO-LINE LABEL — user reported that "ZEUS REFRESHING" on a single line wrapped awkwardly with "ZEUS" floating short above "REFRESHING", unbalanced. Restructured label into two explicit lines in JSX: .ptr-line-top ("ZEUS") stacked above .ptr-line-bot ("REFRESHING"), both centered on the same vertical axis via flex column. CSS: .ptr-label becomes flex-direction: column, align-items: center. Removed textContent mutations in updateInd/onTouchEnd since the markup is now static — mutating would flatten the nested spans back to one line. Indicator height bumped 50px → 54px to fit both lines comfortably; both lines share same font-size/weight/color (#00ff88 bold), small margin-top between them. Previous: batch13 b59 v1.7.33 — Phase 10.4 PTR INDICATOR VISUAL CLEANUP — user reported that the refreshed PTR gesture worked but the two-ring spinner (outer translucent ring + rotating arc on top-border) visually read as "two overlapping circles" over the label, making the text hard to read. Replaced the spinner DOM entirely with a text-only indicator: single "ZEUS REFRESHING" label in green (#00ff88, bold, 12px, 2px letter-spacing). Removed ptr-spinner CSS rules, armed/refreshing color overrides, and the ptr-spin keyframes. Added a subtle opacity pulse via ptr-pulse @keyframes only during the .refreshing state so the user still gets motion feedback while the reload fires. updateInd/onTouchEnd in PanelShell.tsx no longer query or manipulate .ptr-spinner. Previous: batch12 b58 v1.7.32 — Phase 10.3 PULL-TO-REFRESH IN PANELS RESTORED — user reported that the legacy pull-to-refresh gesture worked anywhere in the old app but stopped working once you opened any dock panel. Audit trace (PanelShell.tsx:181+, app.css:5655+, 3342+): code was intact but blocked by the PageView overlay introduced with the React migration. Four independent blockers: (1) G5 isInsideScrollable saw .zpv-content (overflow-y:auto) as a "scrollable child" and auto-cancelled at touchstart; (2) getScrollTop read window.scrollY while the real scroll lived inside .zpv-content; (3) #ptr-indicator z-index 150 was hidden under .zpv z-index 900; (4) slideContent transformed <main> which was covered by the overlay, so the animation was invisible. FIX: detect the active scroller per-gesture (.zpv .zpv-content when panel open, else window/<main>) and pass it through getScrollTop/isInsideScrollable/slideContent/resetAll; isInsideScrollable stops its walk at the active scroller instead of treating it as a block; bumped #ptr-indicator z-index to 951 + added .zpv ~ #ptr-indicator { top: 48px } so the indicator anchors below the panel header; added a capture-phase scroll listener so momentum guard still arms for .zpv-content scrolls. LABEL: all three PTR states now read "ZEUS REFRESH"; state is communicated via color (.armed → green) and spinner animation (.refreshing → spin). Previous: batch11 b57 v1.7.31 — Phase 10.2 CLOSE-ALL DOUBLE-ATTACH FIX — the Manual panel CLOSE ALL button skipped its two-click confirm and closed all manual positions on the first click. Root cause: attachConfirmClose was called twice on the same #closeAllBtn element (bootstrapMisc.ts:268 timeout init + ManualTradePanel.tsx:121 React useEffect). Each call registered its own click listener. First click fired BOTH listeners in order — listener A set _pendingClose; listener B saw the now-populated entry and fired the callback. FIX: events.ts attachConfirmClose now marks the button with data-confirm-close-attached="1" on first wire and short-circuits subsequent attaches (keeps pending-style re-apply for React re-renders so a mid-confirm re-mount does not lose the visual cue). Phase 10.1 carried over: batch10 b56 v1.7.30 — Phase 10 ORDER/PLACE OWNERSHIP CLASSIFICATION — fixes the root of the "AT positions show in Manual" bug that survived Phase 9. Server /api/order/place auto-called registerManualPosition for every MARKET fill, and registerManualPosition HARDCODED autoTrade:false, sourceMode:"manual", controlMode:"user" — so even AT-originated orders (liveApiPlaceOrder from autotrade.ts) landed in the Manual panel once the server snapshot overrode the client _mapServerPos existingPos preservation. FIX: [10A] autotrade.ts liveApiPlaceOrder call now passes leverage:lev (was missing → server defaulted lev to 1, the x1 bug) + source:"auto". [10B] liveApi.ts manualLivePlaceOrder now forwards source:"manual". [10C] trading.js /order/place forwards req.body.source into registerManualPosition data. [10D] serverAT.js registerManualPosition derives autoTrade/sourceMode/controlMode from data.source — "auto" → autoTrade:true, sourceMode:"auto", controlMode:"auto"; anything else → manual defaults (backward-compatible with /api/at/register-manual which never sets source). Log tag switches between AUTO/MANUAL. Test sweep: tsc 0, vitest 79/79, vite build OK. Previous: batch9 b55 v1.7.29 (Phase 9 position ownership + panel parity).'
};
