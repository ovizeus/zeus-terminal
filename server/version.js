// Zeus Terminal — App Version
// Update this file on each deploy
'use strict';

module.exports = {
    version: '1.7.0',
    build: 26,
    date: '2026-04-18',
    changelog: 'Post-v2 batch3-K b26 WIDGET — Android home-screen widget V3 Combo. Resizable widget: small (2x1 BTC-only), medium (2x2 BTC+portfolio), large (4x2 BTC+ETH+portfolio+brain). Layouts auto-switch via onAppWidgetOptionsChanged reading minWidth/minHeight. Native Java: ZeusWidgetProvider (prices from Binance public API, portfolio snapshot from SharedPreferences, click on root opens zeus-terminal.com, click on refresh icon triggers immediate re-render), ZeusWidgetPlugin @CapacitorPlugin exposing updateSnapshot(JSObject) to webview. Bridge: new client/src/core/widgetSync.ts gates on Capacitor.isNativePlatform(), builds snapshot from AT/TP/BM globals, calls plugin every 30s + on zeus:atStateChanged + on visibility change. Layouts: Zeus palette (gold #f0c040 header, dark gradient bg, green/red PnL, gray labels). Registered in MainActivity.onCreate + AndroidManifest <receiver> with zeus_widget_info.xml (resizeMode horizontal|vertical, updatePeriodMillis 1800000 / 30min). APK published to /download/zeus-terminal.apk (8.4MB debug build, JDK 21 + Android SDK 36). server.js: added .apk Content-Type header + Content-Disposition attachment. Zero logic changes to AT/brain/DSL/Zeus core — widget is additive observation-only surface. Previous: batch3-J b25 snooze pill active-state.' — welcome snooze pill active-state feedback. User feedback: "cand selectezi orele butonul acelei ore sa ramana aprins nu are efect vizual". Added snoozePicked state: clicked pill gets .wlc-snooze-active class (gold gradient fill, dark text, 650ms pulse animation scaling 1→1.08→1), other pills dim to opacity .35 disabled, all buttons non-clickable during the 650ms delay, then modal closes. CSS: wlcPillPulse keyframe, :disabled styling, active-override even on disabled so picked pill stays visually lit. Files: WelcomeModal.tsx (+snoozePicked state, +setTimeout 650ms, +conditional className+disabled), app.css (+.wlc-snooze-active, +.wlc-snooze-pill:disabled, +@keyframes wlcPillPulse). Previous: batch3-I b24 — welcome modal snooze (user request: panel apare la fiecare refresh). Added discrete "Don\'t show for…" link under ENTER TERMINAL button (Enter button UNCHANGED per user directive). Click reveals 3 pills: 5h / 12h / 24h + cancel. Selection writes localStorage[zeus_wlc_snoozeUntil] = now + hours*3600000 and closes modal. _showWelcomeModal() gate: checks snoozeUntil at top — if now < snoozeUntil, sets _wlcShown=true and returns silently (skips all DOM/data work). No auto-gate, only manual snooze per user preference. Files: WelcomeModal.tsx (useState + snooze handler + footer markup), bootstrapMisc.ts (localStorage gate), app.css (.wlc-snooze-* styles matching wlc- dark/gold palette). Previous: batch3-H b23 — audit AT/brain/DSL cu 11 fix-uri surgical-minimale (zero schimbări de logică sau flow). #1 brain-AT race: verificat — AT.running + snapshot per-funcție deja asigură atomicitate. #2 kill-switch reentry: guard-ul citește acum AT.killTriggered direct (sincron) în loc de getATKillTriggered() (async prin store event). #3 SERVER_AT flip: log explicit când _serverATEnabled comută LOCKED/UNLOCKED. #4 DSL mode silent fallback: atLog info→warn + console.warn ca userul să vadă că s-a setat ATR default. #5 blank catches FUSION_CACHE/FUSION_LAST/WR_FILTER: acum log console.warn în loc să swallow silent. #6 brain 68 hardcoded extras în BRAIN_READY_SCORE + BRAIN_MIN_SIGNALS const. #7 dual-store drift: invariant check AT.enabled (legacy) vs useATStore.getState().enabled la începutul fiecărui AT tick, log + re-emit dispatch dacă detectat. #8 post-close PnL merge: log warn când closeDemoPos nu setează _closePnl (fallback pe pnl2 pre-close). #9 stale positions (BUG-08 a scos 24h): atLog warn + console.warn când poziție deschisă 7+ zile — NU auto-close. #10 BM.confluenceScore not-ready fallback la 50: throttled console.warn la 60s. #11 clientOrderId AT_ isolation: random de la 2 cifre (1-in-100 collision/ms) la 6 cifre + underscore separator. Files: autotrade.ts, liveApi.ts, brain.ts, state.ts. Previous: batch3-G b22 cache-proof z-index settings modals. LIQ/LLV/SUPREMUS settings modals opened BEHIND the Select Indicator panel. .mover (modal overlay) was z-index:1000, .ind-panel was z-index:8000, so every mover-based modal (mliq, mllv, mzs, msupremus, ovi) opened behind the panel while generic indicator settings (z-index 9500) opened in front. Bumped .mover to z-index:9000 — now above ind-panel (8000) and below ind-settings-modal (9500) and cmd-overlay (99999). Previous: batch3-D b19 overlay root-cause fix (reuse React chart, stop creating invisible ghost chart). Previously initCharts() in marketDataChart.ts unconditionally called LightweightCharts.createChart(el("mc")) a second time, appending a ghost canvas as a sibling of React TradingChart inside #mc. Because #mc has overflow:hidden;height:400px, the second canvas was clipped invisible. w.mainChart pointed to the invisible chart, so every overlay (renderHeatmapOverlay, renderSROverlay, renderZS, renderLiqLevels, renderOviLiquid) silently drew into a hidden canvas. Fix: initCharts now detects an existing w.mainChart (registered by TradingChart via chartBridge) and reuses it; series creation is idempotent (only creates ema50S/ema200S/wma20S/wma50S/stS/volS when not already registered), always creates the Zeus-specific ema3S/ema4S extensions, and guards the subscribeVisibleLogicalRangeChange hook behind _chartSyncInstalled so it fires only once. CVD chart is also guarded by !w.cvdChart to stay idempotent across hot-reloads. Previous: batch3-C b18 EMA p3/p4 + overlay-toggle hardening. IND_SETTINGS.ema now has p1/p2/p3/p4 in both client state.ts and public/legacy state.js; openIndSettings shows all four Period inputs (labels p3=Period 3, p4=Period 4). marketDataChart.initCharts creates two new line series (w.ema3S #00ff88, w.ema4S #ff66cc) and renderChart feeds them from IND_SETTINGS.ema.p3/p4 (0 = hidden). engine/indicators applyIndVisibility toggles ema3S/ema4S visibility with ema. phase1Adapters seeds w.ema3S/w.ema4S = null before initCharts runs to keep typeof checks safe. PanelShell chart legend shows EMA P1/P2/P3/P4 with matching colors. ChartControls togOvr now trusts legacy w.S.overlays[key] as source of truth (reads the new value AFTER togOvrFn runs so React state can never lag behind the legacy store), and the sync useEffect mirrors w.S.overlays → React store every 2s so toggles that mutate only the legacy side (persistence, cross-module bumps) show up in the React UI within ≤2s. Previous builds 8-17.'
};
